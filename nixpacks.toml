# Nixpacks configuration
[phases.build]
cmds = ["python manage.py collectstatic --noinput"]

[phases.setup]
nixPkgs = ["python311", "postgresql"]

[start]
cmd = "python manage.py migrate && python manage.py seed_data && gunicorn studydashboard.wsgi"
